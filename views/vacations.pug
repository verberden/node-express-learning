extends layouts/main.pug

block content
    h1 Weekend tours
        each vacation in vacations
            .vacation
                h3 #{vacation.name}
                p #{vacation.description}
                if vacation.inSeason
                    span.price vacation.price
                    a(href="/cart/add?sku=" + vacation.sku class="btn btn-default") Buy now!
                else
                    span.outOfSeason We are sorry, but it's not a season for this tour.
                    a(href="/notify-me-when-in-season?sku=" + vacation.sku) Let me know when the tours season starts.
    hr
    p Currency
        a(href="/set-currency/USD", class='currency '+currencyUSD+'') USD
        a(href="/set-currency/GBP", class='currency '+currencyGBP+'') GBP
        a(href="/set-currency/BTC", class='currency '+currencyBTC+'') BTC